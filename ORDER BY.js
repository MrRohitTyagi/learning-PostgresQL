`⭐ ORDER BY query on a table`;

`👉 get sorted data based on any column name in a table`;

`command :`;

`select  * from "Tablename" ORDER BY "column name"`;

`example: select  name  from emp order by salary`;

`NOTE: this sorts asscending by default, to sord descend wise use "DESC or  add minus(-) sign before column name"`;

`example 1: select  name  from emp order by -salary`;
`example 2: select  name  from emp order by salary DESC`;
