"⭐ AGGRIGATE FUNCTIONS on a table";

`👉 THis is used when we want to perform some calculations ont he table records
 ie:  COUNT,SUM,MIN,MAX,AVG
`;

("COUNT");
`select count(*) from emp;`;
`count
26`;

("select SUM(salary),max(salary),min(salary),avg(salary) from emp;");

`"sum"	"max"	"min"	"avg"
2231000	1000000	5000	85807.692307692308`;
